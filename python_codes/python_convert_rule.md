# Python 変換ルール

C++ のサンプルコードを Python に変換するときは、情報の入力には `input()` を優先して使います。
型宣言・ループ・配列操作などは Python らしく簡潔にしつつ、元コードの変数名や処理順はできるだけ保つ。
より小さい比較を行うとき，chmin(a, b)やchmax(a,)のように，chmin,chmax関数を使わず，a = min(a,b)の書き方でpythonに変換すること
